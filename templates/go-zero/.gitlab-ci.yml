# GitLab CI/CD 配置
# Go-Zero 服务自动构建和部署

stages:
  - test
  - build
  - deploy

variables:
  # 镜像仓库地址（根据实际修改）
  REGISTRY: localhost:5050
  IMAGE_NAME: $CI_PROJECT_PATH
  IMAGE_TAG: $CI_COMMIT_SHORT_SHA
  # Go 配置
  GOPROXY: https://goproxy.cn,direct
  GO111MODULE: "on"

# 缓存 Go 依赖
cache:
  key: ${CI_COMMIT_REF_SLUG}
  paths:
    - .go/pkg/mod/

# ============ 测试阶段 ============
test:
  stage: test
  image: golang:1.21-alpine
  before_script:
    - apk add --no-cache git make
    - mkdir -p .go
    - export GOPATH="$CI_PROJECT_DIR/.go"
  script:
    - go mod download
    - go vet ./...
    - go test -v -race -coverprofile=coverage.out ./...
    - go tool cover -func=coverage.out
  coverage: '/total:\s+\(statements\)\s+(\d+.\d+)%/'
  artifacts:
    reports:
      coverage_report:
        coverage_format: cobertura
        path: coverage.out
  only:
    - merge_requests
    - main
    - master

# 代码检查
lint:
  stage: test
  image: golangci/golangci-lint:v1.55-alpine
  script:
    - golangci-lint run --timeout 5m
  allow_failure: true
  only:
    - merge_requests

# ============ 构建阶段 ============
build:
  stage: build
  image: docker:24
  services:
    - docker:24-dind
  variables:
    DOCKER_TLS_CERTDIR: ""
  before_script:
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
  script:
    - docker build -t $REGISTRY/$IMAGE_NAME:$IMAGE_TAG .
    - docker tag $REGISTRY/$IMAGE_NAME:$IMAGE_TAG $REGISTRY/$IMAGE_NAME:latest
    - docker push $REGISTRY/$IMAGE_NAME:$IMAGE_TAG
    - docker push $REGISTRY/$IMAGE_NAME:latest
  only:
    - main
    - master
    - tags

# ============ 部署阶段 ============
# 部署到开发环境
deploy_dev:
  stage: deploy
  image: bitnami/kubectl:latest
  script:
    - kubectl config use-context dev
    - sed -i "s|IMAGE_TAG|$IMAGE_TAG|g" deploy/k8s/deployment.yaml
    - kubectl apply -f deploy/k8s/
  environment:
    name: development
    url: http://dev.api.local
  only:
    - main
    - master

# 部署到生产环境（手动触发）
deploy_prod:
  stage: deploy
  image: bitnami/kubectl:latest
  script:
    - kubectl config use-context prod
    - sed -i "s|IMAGE_TAG|$IMAGE_TAG|g" deploy/k8s/deployment.yaml
    - kubectl apply -f deploy/k8s/
  environment:
    name: production
    url: http://api.local
  when: manual
  only:
    - tags

# ============ 使用 ArgoCD 部署（可选） ============
# deploy_argocd:
#   stage: deploy
#   image: argoproj/argocd:latest
#   script:
#     - argocd app sync $CI_PROJECT_NAME --revision $CI_COMMIT_SHA
#   only:
#     - main
